#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

bool isOperator(int op) {

}

int priority(int op) {

}

int calculate(int op, int operand1, int operand2) {

}

int main() {
    char expression[100];

    printf("Enter expression in infix notation: ");
    gets_s(expression);

    return 0;
}